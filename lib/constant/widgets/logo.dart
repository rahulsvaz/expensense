import 'package:flutter/material.dart';

class LogoExpensense extends StatelessWidget {
  const LogoExpensense({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Text('EXPENSENSE',style: TextStyle(fontSize: 34,color: Colors.white,fontWeight: FontWeight.bold), );
  }
}
